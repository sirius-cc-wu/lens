# User Prompts

1. Please read @~/Github/obsidian-puml-viewer/ . I'd like to have a viewer also for Markdown files with PlantUML blocks. It's a CLI which when run in a codebase will open a browser to display the codebase's code and document. It may take a file as its argument, or a directory, or without any argument, which then means the current repository. It should not depend on Obsidian. I'd like to discuss with you about its architecture, its spec, etc.

2. The CLI will be named 'lens'.
